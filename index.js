'use strict'

module.exports = async function (config) {
  const Agent = {}
  const Metric = {}
  //
  return {
    Agent,
    Metric
    // Use previous Emc Javascript
    // Agent : Agent,
    // Metric : Metric
  }
}
